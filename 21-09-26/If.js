const prompt =  require ('prompt-sync') ();


console.log ("---- Desconto em compras ---- ");

let = descontoCompras =Number(prompt('"Digite o valor da compra: '));

if(descontoCompras >=100) {
    console.log("Você recebeu desconto de R$20! ")
} else(descontoCompras >100);(
    console.log("Você não recebu desconto. ")
);

